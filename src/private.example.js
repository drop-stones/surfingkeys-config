// private settings

// keys
const unmaps = {
  // domain: { normal: [], insert: [], visual: [] }
}
const maps = {
  // domain: { normal: [], insert: [], visual: []}
  // - map(new_keystroke, old_keystroke[, domain, new_annotation])
  // - mapkey(keys, annotation,, jscode[, object])
}

// search engines
const search_engines = [
  // { alias, prompt, search_url, suggestion_url, callback_to_parse_suggestion }
]

export {
  unmaps,
  maps,
  search_engines,
}
